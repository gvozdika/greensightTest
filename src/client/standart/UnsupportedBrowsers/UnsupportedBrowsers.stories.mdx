import { Meta, Story } from '@storybook/addon-docs/blocks';
import centeredDecorator from '@decorators/centeredDecorator';

import UnsupportedBrowsers from '../UnsupportedBrowsers';

<Meta title="Standart / UnsupportedBrowsers" component={UnsupportedBrowsers} decorators={[centeredDecorator]} />

# UnsupportedBrowsers

<Story name="Knobs">
    <UnsupportedBrowsers />
</Story>

Простой компонент для вывода на сайте вместо контента в неподдерживаемом браузере .
