import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / Accessibility" />

# Accessibility

Проекты на RWBP разрабатываются с "accessibility in mind", так что при разработке нужно придерживаться заложенных концептов.

☝🏿 Главное, что следует понимать про a11y - это то, что применяемые паттерны затрагивают не только определённую группу людей (например, с нарушениями зрения), а касаются многих областей. Следование этим паттернам повышает качество UX для всех пользователей, открывает новые возможности для power users и даже косвенно затрагивает SEO, т.к. многое завязано на семантике.

## Разработка компонентов

При разработке компонента или страницы нужно держать в голове, как минимум, управление с клавиатуры и восприятие разметки скринридерами.

Особенно это важно для базовых элементов из `GDS`, т.к. они повсеместно переиспользуются и должны иметь "baked in accessibility". При построении таких компонентов самым важным документом является спецификация [WAI-ARIA](https://www.w3.org/TR/wai-aria-practices/), в котором для каждого типового элемента расписано какой он должен поддерживать функционал, как компонент должен управляться с клавиатуры, как он взаимодействует с AT, и приведены примеры реализации.

## Focus Management

Для работы фокуса подключен полифилл для `focus-visible`: [focus-visible](https://github.com/WICG/focus-visible). Он позволяет отделить поведение фокуса клавиатуры от других видов фокуса, так что появление стилей фокуса после клика по кнопке более не является проблемой. Чтобы применить стили фокуса для клавиатуры на блоке с классом `.block` вместо `:focus`, который применится на все виды фокуса, необходимо прописать селектор:

```css
.block {
    .js-focus-visible &:focus.focus-visible {
    }
}
```

❗️ Это единственное исключение, когда класс с префиксом `js-` используется внутри css.

Общий подход к стилизации фокуса:

1. отключить дефолтные стили фокуса в `base.css`
1. определить общие стили фокуса для клавиатуры в `base.css`
1. определить более "fine grained" стили фокуса для отдельных компонентов по необходимости

## AT Support

`AT` - это assistive technologies, то есть вспомогательные технологии для просмотра сайта. Самым известным является скринридер, зачитывающий контент страницы пользователю по мере навигации его по странице через клавиатуру.

Для поддержки `AT` должна соблюдаться семантичность разметки и проставляться нейминги.

### Семантика

Ключевые моменты для семантики:

1. использование `landmarks` - разметки блоков через теги main, aside, nav, article и пр. вместо нейтральных div
1. использование каскадной структуры заголовков без пропусков уровней - скринридеры строят меню из этих заголовков
1. соблюдение нормативов, определённых спецификацией, при построении базовых элементов, в том числе это включает добавление aria-атрибутов

### Нейминги

Нейминги важны, т.к. скринридеру нужен текст, чтобы озвучить его пользователю. По этой причине всем изображениям прописывается атрибут `alt`, для svg, используемых внутри кнопок, присваивается `title`, инпуты связываются с `label` и т.д.

Имена могут указываться напрямую вложением текста внутрь тега, через атрибуты `title` и `alt` (для изображений), а также через `aria-label` и `aria-labelledby`. Проверить присваемое элементу имя можно через инструменты разработчика в Chrome: `F12 -> Elements -> Accessibility`.

## Скиплинки

В RWBP добавлен компонент скиплинков - это ссылки наверху страницы, видимые только при переходе на них через Tab, которые ведут на ключевые разделы страницы. Такие ссылки облегчают работу с сайтом пользователям клавиатуры. Подробнее про них можно почитать в документации компонента `Skip`.

## VisuallyHidden

Бывает необходимо скрыть текст визуально, когда он и так понятен из контекста, но оставить его доступным для AT. Или предоставить пользователям AT дополнительную информацию, которая для этого должна присутствовать в разметке, но визуально показываться не должна. Для этих целей служит компонент `<VisuallyHidden />` из GDS, в который нужно оборачивать такие элементы вместо `display: none` или `visibility: hidden`.

## Контроль качества

A11Y включает в себя множества правил, которым необходимо следовать при разработке и проектировании интерфейсов. В этом документе затронута лишь часть, наиболее важная для фронтенда. Эти правила не взяты с неба, а строго определяются спецификацией. Для их контроля существуют чеклисты, наглядно их перечисляющие: [WCAG Checklist](https://www.wuhcag.com/wcag-checklist/).

Для компонентов, созданных в Storybook, проверка ряда правил автоматизирована через `addon-a11y`, использующий [axe](https://www.deque.com/axe/) для тестов accessibility. Автоматизированные тесты не гарантируют работоспособности конечного продукта, но покрывают 30-50% правил. Для остального нужно производить ручное тестирование. Например, проверку скринридеров невозможно автоматизировать - их нужно ставить и пробовать самим.

Для компонентов вне Storybook, например целых страниц, можно для тех же целей использовать браузерные расширения. Тот же `axe` может быть встроен в качестве [chrome extension](https://chrome.google.com/webstore/detail/axe-web-accessibility-tes/lhdoppojpmngadmnindnejefpokejbdd).

☝🏿 Без тестов на уровне страниц соблюдение структуры заголовков, например, вам не проверить.
