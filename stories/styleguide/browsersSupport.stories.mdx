import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / Browsers Support" />

# Browsers Support

Список браузеров не должен бездумно копироваться из проекта в проект, а зависит от бизнес-требований, определяясь на этапе разворота проекта через предоставленный менеджером список.

Рекомендуемый список браузеров лежит в [Notion](https://www.notion.so/ed0190c456f34777a0ebba0609c578ad?v=c67f91d1a21945ef83421b70282d7fb3).

⛔ RWBP не поддерживает IE11. Если вам потребовалась его поддержка, то добавить его в Browserslist будет недостаточно. Необходимо будет изменять ряд корневой логики и отказываться от части функционала.

## Browserslist

[Browserslist](https://github.com/browserslist/browserslist) - это инструмент, позволяющий хранить список поддерживаемых браузеров в едином месте. Ряд инструментов, завязанных на списке браузеров, знают о его существовании и берут конфиг из него. Конфиг хранится в `package.json` в поле `browserslist`.

Инструменты, использующие `Browserslist`:

1. **babel**. Транспайлинг ES6+ синтаксиса через `@babel/preset-env` и подгрузка полифиллов через `core-js`.
1. **autoprefixer**. Генерация vendor-префиксов.

☝🏿 Список браузеров единый для `development` и `production`, так что проект может спокойно тестироваться на девайсах в dev-моде с возможностью внесения быстрых правок с `webpack-dev-middleware`.

## Unsupported browsers

Вместо контента в неподдерживаемых браузерах выводится рекомендация скачать современные браузеры. Пока что тут вручную дублируется логика browserlist. В scripts/helpers есть ф-ция getIsBrowserSupported.

## Babel

На проекте используется современный синтаксис `ES6+` (let/const, шаблонные строки, стрелочные функции, деструктуризация, rest/spread операторы, async/await). Поддержка старых браузеров осуществляется через транспайлинг при помощи `Babel`, его настройки прописаны в `babel.config.js`. Для автоматической поддержки всех современных возможностей используется пресет `@babel/preset-env`. Экспериментальный синтаксис подключается отдельно через плагины.

И пресет и полифилл зависят от `Browserslist`, так что подтягивают только необходимый для указанного списка браузеров код.

Полифиллы подгружаются автоматически только для непосредственно используемого в коде функционала, который невозможно просто затранспайлить. Полифиллы добавляются с помощью `core-js` с опцией `useBuiltIns: usage`.

❗️️ `useBuiltIns: 'usage'` не грузит полифиллы для вендорного кода, так что, если разработчики библиотеки не встроили полифиллы, то их нужно подключить напрямую в `src/client/index.tsx`, например `import 'core-js/modules/es.object.from-entries'`. Большую часть можно найти в `сore-js` и подключить прямым импортом.

☝🏿 Импорты обрабатывает `webpack`, поэтому ставится `modules: false`. При этом, чтобы было можно использовать импорты в webpack-конфигах, они добавляются в исключения через `overrides` и в расширения файлов прописывается "babel".
