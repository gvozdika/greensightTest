import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / Concepts" />

# Concepts

Здесь описывается предполагаемая логика процесса разработки под RWBP.

## Создание компонента

Изначально у нас есть макет и список требований к компоненту. Сначала мы реализуем его статичную версию в изоляции от приложения. Для этого используется Storybook:

```
yarn storybook
```

Компонент создаётся внутри `components` в отдельной именной директории. Структура типовой директории следующая:

-   **index.tsx** - основной код компонента. Называется index для удобного импорта
-   **Example.stories.mdx** - представление в Storybook

Это обязательный набор, могут ещё быть дополнительные файлы, например, уникальные хелперы и переопределение стилей плагина.

При разработке статичного компонента все его пропсы описываются в `d.ts` файле и выводятся в сторис `Knobs`. Отдельные сторисы создаются под отдельные юзкейсы. В качестве обработчиков вешаются `actions`.

☝🏿 Зачастую компонент настолько простой, что knobs для него даже не нужны. Это норма.

Т.о. компонент разрабатывается без зависимостей от бизнес-логики, запросов, стора. На него не влияет конфиг приложения, SSR, роутинг и прочие внешние факторы. Наша цель на данном этапе - создать крепкий независимый компонент, для которого определена структура, стилизация, набор параметров, обработчики, основные юзкейсы.

По завершению создания компонента мы можем интегрировать его в приложение. В простейших случаях непосредственно, в более сложных через контейнер.

Для работы с приложением запускаем dev-сборку:

```
yarn dev
```

Storybook при этом закрывать не нужно, он выводится на другом порту, так что можно вносить правки в статику параллельно.

На dev-сборке уже доступно реальное окружение с SSR, роутингом, стором и всеми провайдерами, так что можно понять, как компонент функционирует в рамках системы. Но при этом работают `webpack-dev-middleware` и `HMR`, обеспечивающие быструю разработку.

dev-сборка не ставит ограничений на `Browserslist`, так что может тестироваться на девайсах по вашему IP, прописываемому в консоли.

После того как бизнес-логика будет добавлена, и компонент займёт своё место на странице, остаётся протестировать его на prod-сборке:

```
yarn prod
```

Если ничего не развалилось, то можно пушить 😎

## Создание контейнера

Если компонент для использования на проекте требует наличие какой-либо бизнес-логики (например, связки с redux store), то для него создаётся контейнер в директории `containers`. Такой подход позволяет использовать в Storybook статичную версию, и динамическую на проекте. Именно в контейнере прописываются `mapStateToProps` и `mapDispatchToProps`.

## Создание страницы

Страницы никак не фигурируют в Storybook, т.к. непосредственно относятся к бизнес-логике, завязаны на роутинге и SSR.

Под каждую страницу выделяется отдельная директория внутри `pages`. Страница – это такой же компонент, как и остальные, которая используется в `App.js` в качестве роута.
