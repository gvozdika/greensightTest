import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / Images optimization" />

# Images optimization

☝🏿 Документация по оптимизации изображений тесно переплетается с документацией компонента `Picture`, так что за примерами обращайтесь туда.

Изображения можно разделить на **контентные** и **неконтентные**.

**Контентные** - это изображения, приходящие из API, задающиеся из админки, заливаемыми контент-менеджерами. Над этими изображениями фронтенд не имеет абсолютно никакой власти, и контроль их размеров ложится на людей, их публикующих, и бэкенд, который теоретически может автоматизировать их оптимизацию.

☝🏿 Инструменты для ручной оптимизации приведены в полезных ссылках в конце этого документа.

❗️ Если аудиты показывают плохие результаты, то велика вероятность, что это происходит именно из-за контентных изображений. RWBP никак не застрахует вас от залива изображения в метр весом.

☝🏿 Имейте в виду, что значимость оптимизации изображений в целом переоценена. Куда больший вклад оказывает оптимизация js-кода, т.к. он исполняется каждый раз. Однако если вы ориентируетесь только на цифры аудитов, то изображения будут вам казаться первостепенными, т.к. значительно понижают score. Это вызвано тем, что аудиты симулируют первое открытие сайта. Однако тот момент, что при повторном открытии изображения просто возьмутся из кэша, они не учитывают.

**Неконтентные** - это изображения, встроенные в код и недоступные для изменения извне. Это изображения статичных компонентов и декоративные изображения, являющиеся частью дизайна. Над этими изображениями фронтенд имеет полную власть и именно их оптимизации посвящена эта дока, хотя приводимые ниже категории оптимизаций применимы и для контентных изображений и о них просто полезно знать.

## Виды оптимизации

Можно выделить 4 основных вида: сжатие, webp, адаптивные версии и ленивая загрузка.

### Сжатие

Сжатие ассетов - это самое простое и необходимое, что нужно делать с изображениями. В `webpack` сжатие реализуется через [imagemin-webpack-plugin](https://github.com/Klathmon/imagemin-webpack-plugin) - плагин, объединяющих инструменты для сжатия различных форматов. В конечном итоге это:

1. **mozJpeg** для jpeg. Позволяет делать lossy-сжатие (сжатие с потерями), которое даёт лучший результат против `jpegtran` (lossless-сжатие)
1. **pngquant** для png. Аналогично это lossy-сжатие в отличие от optipng
1. **svgo** для svg. Обработка svg подробно разобрана в `Icons`

### Генерация WebP

WebP - это современный формат изображений, предоставляющий лучшее сжатие, нежели обычные jpeg/png. На момент написания документации поддерживается во всех современных браузерах кроме Safari: [Caniuse](https://caniuse.com/#feat=webp).

Генерация WebP происходит автоматически на этапе сборки в production через [imagemin-webp-webpack-plugin](https://github.com/iampava/imagemin-webp-webpack-plugin).

Добавление WebP никак не отменяет использования обычных jpeg/png, т.к. поддержка есть не везде. Поэтому вставка идёт через тег picture, который автоматически делает фолбек на обычное расширение, если в браузере нет поддержки.

### Адаптивные изображения

Для крупных изображений есть смысл создавать версии с меньшим разрешением для небольших экранов. Загрузка изображения в 1920 x 1080 на экране шириной в 320px не даст никакого профита, только убьёт траффик и отложит его загрузку.

Сборщик позволяет грузить такие изображения через директорию **images/responsive**, указывая при импорте список размеров, для которых нужно сгенерировать изображения. После чего на production-сборке он создаст их через [responsive-loader](https://github.com/herrstucki/responsive-loader), использующий `jimp`.

### Ленивая загрузка

Ленивая загрузка реализуется через библиотеку [lazysizes](https://github.com/aFarkas/lazysizes).

Для предоставления плейсхолдеров используется паттерн `SQIP`: svg изображение, собранного из примитивов с применённым фильтром блюра конвертируется в URL и вставляется инлайном.

Плейсхолдеры генерируются на production автоматически через [sqip-loader](https://github.com/EmilTholin/sqip-loader).

🥇 Не нужно ставить всем изображениям lazy. В частности ни к чему делать верхние изображения страниц (hero images) ленивыми - это только ухудшит UX. Более того, изображение, вставленное через lazy, при повторном заходе на страницу, несмотря на кэш, будет производить подмену через javascript (выкачивать заново не будет конечно, но визуально заметно), когда обычное при повторном заходе будет сидеть как влитое, без скачков, сразу на странице.

## Выбор правильного формата

`Webpack` обрабатывает изображения только в форматах jpeg/png, webp напрямую не грузится. Для фото предпочтительнее использовать jpeg, т.к. он в целом в итоге даёт меньший вес, а png использовать для изображений, требующих поддержку полупрозрачности.

### Анимированные изображения

Если у вас возникла идея загрузить на сайт gif, забудьте об этом. Gif, несмотря на его популярность в интернете, не предназначен для веба и имеет гигантские размеры - настолько гигантские, что ему даже посвящён отдельный аудит в Lighthouse.

Вместо gif используйте обычное видео. Разработчик вставит его через тег video и стилизует так, как будто это типичная гифка - без панели управления с автоплеем.

Формат видео должен быть WebM с фолбеком на MP4. WebM - это как WebP, только для видео. Его поддержку можно проверить на [Caniuse](https://caniuse.com/#feat=webm). Если браузер не поддерживает WebM, то загрузится MP4.

Пример разметки для псевдогифки:

```js
<video autoplay loop muted playsinline>
  <source src="example.webm" type="video/webm">
  <source src="example.mp4" type="video/mp4">
</video>
```

## Контроль качества

Своевременный контроль качества обработки изображений позволит вам избавиться от неожиданных просадок по аудитам. Нужно регулярно проводить проверку либо через `Lighthouse` в Chrome, либо через специализированный чисто на изображениях сервис [Image Analysis Tool](https://webspeedtest.cloudinary.com) от Cloudinary, который сразу же позволяет скачать оптимильную версию изображения.

## Полезные ссылки

Статьи:

1. [Essential image optimization от Addy Osmani](https://images.guide)
1. [Типовые кейсы по responsive images на Dev.Opera](https://dev.opera.com/articles/responsive-images/)
1. [Responsive Images 101](https://cloudfour.com/thinks/responsive-images-101-definitions/)

Инструменты:

1. [Squoosh](https://squoosh.app/) — инструмент для ручного сжатия и конвертации в WebP
1. [SVGOMG](https://jakearchibald.github.io/svgomg/) — графический интерфейс svgo для ручного сжатия svg
1. [Responsive Image Breakpoints Generator](https://www.responsivebreakpoints.com/) — генератор адаптивных изображений от Cloudinary
