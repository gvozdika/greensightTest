import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / Performance" />

# Performance

Большая часть оптимизаций автоматизирована и производится через `Webpack` на уровне production-сборки. Здесь разобраны применяемые на проекте оптимизации. Остальная оптимизация вроде написания оптимального кода, добавления динамических импортов и сокращения ререндеров / числа запросов лежит на разработчике.

☝🏿 Оптимизация изображений здесь не рассматривается. Это отдельный большой топик, которому посвящён раздел `Images optimization`.

## Minification

Минификация - самый простой и в то же время самый эффективный вид оптимизации. Смысл его в сокращении числа символов в коде при сохранении функционала. При этом код становится нечитаемым, но машина его поймёт без проблем. К техникам минификации относится удаление пробельных символов, удаление комментариев, сокращение имён переменных и более сложное перестроение синтаксических конструкций после анализа AST (например, перевод условного оператора в тернарный или объединение деклараций переменных). Мы производим минификацию **js**, **css** и **html** кода.

Минификация js производится через [terser-webpack-plugin](https://github.com/webpack-contrib/terser-webpack-plugin), использующий [terser](https://terser.org/), который пришёл на смену `uglify-js`. Новые версии по умолчанию активируют опции `cache`, `parallel` и удаляют версионные комментарии вендоров. Настройки можно менять в клиентском prod-конфиге через `optimization/minimizer`.

Минификация css производится через [optimize-css-assets-webpack-plugin](https://github.com/NMFR/optimize-css-assets-webpack-plugin), использующий [cssnano](https://cssnano.co/). Настройки его также могут быть изменены через `optimization/minimizer`.

Под минификацией html понимается обёртка шаблона, прописанного в `server/template.js`, через [html-minifier](https://github.com/kangax/html-minifier). Настройки его прописываются там же.

До применения минификации: 565Kb.<br />
После применения минификации: 270Кб.<br />

## Compression

Сжатие - также простой и эффективный вид оптимизации из категории "must have". Можно воспринимать этот процесс как генерацию архивов, которые возвращаются пользователю по запросу вместо оригинальных файлов. Обычно на сервере настраивается динамическое сжатие запрашиваемых файлов в `GZIP`, но мы пошли дальше и помимо этого генерируем `Brotli` и используем статичное сжатие.

На бэкенде должно быть настроено сжатие файлов. Проверить его наличие можно в инструментах разработчика в Chrome: `F12 -> Network -> выбираем файл -> Headers -> Response Headers`. Если в заголовках есть опция `content-encoding: br`, то отдаётся `Brotli`, если `content-encoding: gzip`, то `GZIP`. Если его нет, то в срочном порядке пинайте бэк!

Современные браузеры научились помимо `GZIP` понимать более эффективный метод сжатия через `Brotli`. Поэтому на наших серверах производится сжатие именно через него и только если браузер его не поддерживает, то вместо него генерируются `.gz` файлы. На момент написания статистика уже очень хорошая и покрывает все браузеры из дефолтного Browserslist: [Caniuse](https://caniuse.com/#feat=brotli).

Степень сжатия различается по уровням. Более высокие дают большее качество, но занимают больше времени. Т.к. на сервере сжатие происходит динамически по запросу пользователя, то используя высокий уровень, мы рискуем потратить на сжатие больше времени, чем ушло бы на отдачу менее качественно сжатого файла. Поэтому на бэке обычно берут средние значения. Однако для статичных типов ассетов, генерируемых на этапе сборки, мы можем позволить себе предварительное сжатие. Это кстати довольно революционная идея и пока мало где используется.

Так что процесс следующий: на этапе prod-сборки Webpack использует [compression-webpack-plugin](https://github.com/webpack-contrib/compression-webpack-plugin), генерируя сжатые версии **js**, **css** и редких незаинлайненных крупных **svg** файлов в форматах `.gz` и `.br` на максимальных уровнях сжатия (9 и 11 соответственно), т.к. мы никуда не спешим. Эти файлы лежат на сервере в директории `build` и при запросе файла сервер проверяет есть ли уже готовая версия и, если есть, просто отдаёт её, не расходуя время на сжатие. Если же файл динамический (сама страница, например), то сжатие выполняется сервером в обычном порядке.

☝🏿 Кстати минификация и сжатие становятся ещё эффективнее, т.к. мы используем `React` на хуках, т.к. код функций сжимается куда эффективнее классов. Да и сам по себе он менее громоздкий.

До применения сжатия: 270Кб.<br />
После сжатия через GZIP: 85Кб.<br />
После сжатия через Brotli: 73Кб.<br />

## Code splitting

Чтобы собрать ваш проект, вебпаку требуется знать энтрипоинты, т.е. точки входа в приложение - для каждой из них он генерирует отдельный бандл. Обычно в качестве энтрипоинта используется страница. Т.к. RWBP предназначен для построения SPA-приложений, то страница у нас всего одна, а следовательно по умолчанию бандлер создаст один огромный бандл на весь проект.

Но это же никуда не годится! Так что используется сплиттинг, т.е. разбиение этого монобандла на чанки. `Code Splitting` - это сплиттинг через динамические импорты, которыми управляет `Webpack`. Т.е. каждый раз, когда вы используете динамический импорт (через скобки) вместо обычного, импортируемый код отделится из общего бандла в отдельный чанк и будет загружен динамически по триггеру, на который вы этот импорт повесите.

Чтобы использовать динамические импорты с `React` в более удобной манере, а также, что более важно, заставить это всё дело работать на SSR, используется [loadable-components](https://loadable-components.com/) - библиотека, используемая в качестве обёртки над динамическими импортами.

RWBP производит сплиттинг на уровне роутов. Все роуты из вашего конфига оборачиваются в `loadable`. Это можно увидеть прямо в `pages/index.js`. Такой паттерн гарантирует, что при открытии страницы загрузится только код этой страницы, а при переходе на другой роут её код будет динамически подгружен через скрипт в head.

Но сплиттинг можно использовать не только для роутов, но и для компонентов, которые грузятся динамически по определённому триггеру. Например, содержимое попапа или тяжеловесного таба.

Например, для попапа:

```js
import React, { useState } from 'react';
import loadable from '@loadable/component';
import { Button } from '@greensight/gds';
import Popup from '@standart/Popup';
import Loader from '@standart/Loader';

const Example = loadable(import(/* webpackChunkName: "Example" */ `./standart/Example`), { loading: Loader });

const SomeComponent = () => {
    const [isPopupOpen, setIsPopupOpen] = useState(false);

    return (
        <>
            <Button onClick={() => setIsPopupOpen(true)}>Open popup</Button>
            <Popup isOpen={isPopupOpen} onRequestClose={() => setIsPopupOpen(false)} contentLabel="Popup with Example">
                <Example />
            </Popup>
        </>
    );
};
```

## Bundle Splitting

Помимо разбиения на чанки по логике через динамические импорты можно выполнять разбиение по типу кода на уровне сборки. Такой подход называется `Bundle Splitting` и прописывается в клиентском production-конфиге через `optimization/splitChunks`. По какому принципу сплитать чанки можно там гибко настроить, но по умолчанию RWBP использует отделение **вендорного** и **общего** кода.

**Вендорные** чанки имеют в названии приписку `vendors` и содержат код 3rd party libraries, т.е. многочисленных подключаемых библиотек. Эти чанки, как правило, самые объёмные. Смысл их отделения в первую очередь в том, что этот код редко меняется, и его можно успешно закэшировать. Подробнее о логике кэширования смотри раздел `Long Term Caching`.

**Общие** чанки, они же коммонсы, имеют в названии приписку `commons` и содержат общий код, который делят между собой несколько чанков. Это нужно, чтобы избежать излишнего дублирования, возникающего при `Code Splitting`. Названия чанков, между которыми код делится, указаны в названии через символ тильды.

☝🏿 Код вендоров не фигурирует в commons, но также делится по принципу общности. Обратите внимание на тильды в названиях вендорных чанков.

## Long Term Caching

Чтобы оптимизировать повторные заходы пользователя на сайт, мы используем кэширование, а точнее `long term caching`. Это подход, при котором хранению кэша ставится максимальное время, а hash-версии прописываются в именах бандлов (у нас используется 4-символьный код перед расширением). Этот hash зависит от содержимого соответствующего чанка и меняется, только если был изменён его контент.

Со стороны пользователя логика следующая. Он заходит на сайт в первый раз -> скачивает все ассеты -> они кладутся в кэш его браузера -> при повторном открытии файлы, которые уже есть в кэше, повторно не выкачиваются. Если он заходит через некоторое время, за которое мы успели залить изменение, затрагивающее отдельную страницу, то hash в названиях файлов изменится только для файлов, имеющих отношение к этой странице, что станет сигналом для браузера выкачать новые файлы, т.к. у пользователя в кэше нет файлов с такими названиями. Все остальные файлы при этом не изменятся. Именно поэтому выгодно проводить отделение вендорного кода от основного кода приложения через `Bundle Splitting`, т.к. вендорный код на этапе поддержки меняется не так часто и просто будет лежать в кэше у пользователя.

Касательно реализации, hash в имена добавляется на этапе prod-сборки - в поле `output` для бандлов и в лоадерах для ассетов.

☝🏿 Чтобы hash не пересчитывался из-за изменений кода самого вебпака, встраимаемого в каждый чанк, этот код выносится в отдельный чанк runtime. Смотри `optimization/runtimeChunk` в клиентском prod-конфиге.

☝🏿 Чтобы hash не пересчитывался из-за изменения числа импортов, мы уходим от построения id модулей на инкрементах. Делается это через опцию `optimization/moduleIds` (раньше это делал `HashedModuleIdsPlugin`, который в новых версиях встроен в качестве опции optimization).

## Monitoring

Помимо добавления оптимизации важно производить мониторинг, быть в курсе проблем производительности на проекте и знать над чем можно поработать.

### Аудиты

Самое очевидное - это проведение регулярных аудитов. Чтобы увидеть реальную картину, их нужно проводить на сервере, но в принципе запуск их на локалке на prod-сборке также даст приблизительно оценить ситуацию (на сервере результат должен быть лучше, в первую очередь засчёт сжатия). Рекомендую использовать связку `Lighthouse` + `WebPageTest`.

`Lighthouse` встроен в Chrome во вкладку Audits. Это позволяет проводить аудиты в том числе на localhost. Через него можно замерить performance (и не только его) и увидеть список проблем со ссылками на возможные решения.

☝🏿 Чтобы увидеть реальную картину, независящую от производительности вашей машины, рекомендуется установка `Throttling`. Обычно достаточно выбрать `Simulated` в настройках аудитов, для финального теста ставьте `Applied`, который будет длиться дольше, но даст более точный результат.

☝🏿 Проводите аудиты в ружиме инкогнито, чтобы расширения браузера не накладывали своих корректив.

[WebPageTest](https://www.webpagetest.org/) используется для более серьёзных тестов на реальных девайсах. По нему можно отследить конкретные проблемы в реквестах, нагрузках и упороться в графики. Также через него можно сгенерировать визуальное сравнение процесса загрузки нескольких версий, сохранив его в видео формате, и показав коллегам.

### Аналитика

`Webpack` генерирует бандлы и позволяет использовать ряд инструментов для их анализа. В сборщик встроен `Webpack Bundle Analyzer` (WBA) - инструмент, интегрируемый в prod-сборку и позволяющий увидеть содержимое ваших бандлов. С помощью него можно узнать какие фрагменты кода куда попадают, какие библиотеки занимают большую часть места и прикинуть как это всё можно оптимизировать. Особенно незаменим он при настройке сплиттинга.

☝🏿 `WBA` не требует запуска отдельного сервера, а просто генерирует статичный файл `report.html`, который всегда можно открыть из корня сервера.

Помимо этого существует множество внешних инструментов, которые позволяют проводить анализ бандлов. Все они работают с файлом `stats.json`, который также генерируется на prod-сборке. Так что для подобных сервисов вы можете просто скормить им этот файл из директории `build`.

Примеры таких инструментов:

1. [Webpack Analyse](http://webpack.github.io/analyse/). Официальный инструмент от Webpack, рисующий граф, выводящий информацию по бандлу и предлагающий ряд улучшений.
1. [Webpack Visualizer](https://chrisbateman.github.io/webpack-visualizer/). Рисует цветастую диаграмму с разбиением на подключаемые модули.
1. [Webpack Chart](https://alexkuz.github.io/webpack-chart/). Альтернативный сервис для построения диаграмм.

### Performance Budget

На тему `performance budgets` написано много статей. Рекомендую прочесть хотя бы классическую от Addy Osmani: [Start Performance Budgeting](https://addyosmani.com/blog/performance-budgets/).

У нас этот процесс не интегрирован на уровне компании и вряд ли когда-то будет. Но на уровне сборки контроль размера бандлов встроен, чтобы держать себя в тонусе. В клиентском prod-конфиге прописано поле `performance` со значениями максимальных размеров чанков, после превышения которых `Webpack` выдаст вам предупреждение на этапе сборки, что пора уже залезть в `WBA` и понять, что можно засплитать или вовсе убрать.

☝🏿 В поле `performance` указываются значения для `maxEntrypointSize` и `maxAssetSize`. Первая метрика в случае SPA довольно бесполезная, т.к. проверяет только совокупность файлов, относящихся к main - нашему единственному энтрипоинту, несмотря на code splitting. А вот `maxAssetSize` обозначает размер отдельного файла, например чанка или шрифта. Размеры указываются для минифицированной версии, но до сжатия. Величина в 300Кб оптимальная, т.к. в меру жёсткая (невиданная для старых проектов с бандлами на 3Мб), но достижимая, т.к. основной вендорный файл в эту величину умещается.
