import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / SEO" />

# SEO

RWBP предназначен для разработки изоморфных веб-приложений, что означает использование SSR в сочетании с клиентским рендером.
Серверный рендер обеспечивает получение разметки страницы при обращении к сайту по url, что даёт необходимую информацию поисковым движкам.

## robots.txt и sitemap.xml

В корне проекта должен быть доступен `robots.txt`. Вы можете удостовериться в его наличии на проекте через url **/robots.txt**.
Этот файл загружается динамически через `GET /robots`, содержимое настраивается со стороны бэкенда.

C `sitemap.xml` ситуация аналогична. Должен быть доступен через url **/sitemap.xml**, загружается через `GET /sitemap`.

## SEO-теги

Для каждой страницы в `head` должен быть задан набор обязательных тегов для SEO.

Минимальный набор:

1. **title** - заголовок страницы
1. **meta name=«description»** - описание страницы
1. **meta name=«keywords»** - ключевые слова

Дополнительно может быть указан **link rel="canonical"**.

💬 **title** и **description** формируют содержимое сниппета в поисковой выдаче. **keywords** имеют меньшую значимость и вероятно даже не учитываются большинством поисковиков.

♿️ **title** и **description** также важны для a11y, т.к. это первый текст, который будет зачитан пользователям AT – по нему можно вынести вердикт о содержимом страницы.

Все эти теги задаются контент-менеджером со стороны админки сайта. Задача фронтенда – предусмотреть получение этих значений и место для вставки. Поэтому был создан компонент `Seo`, предваряющий роутер, в котором происходит запрос к API по `GET /seo` и вставка тегов в `head` через [react-helmet-async](https://github.com/nfl/react-helmet-async).

При отсутствии эндпоинта для SEO (как на Скелетоне) берутся данные из mock-функции в `api/seo.ts` – определяйте там теги, которые будет вставлены через Helmet и не используйте Helmet для этого напрямую (для упрощения дальнейшего перехода на эндпоинт).

На SSR запрос SEO происходит после вызова **loadData**, так что вы можете использовать полученные данные по страницам и вставлять их в полученные шаблоны (см. `reducers/seo.ts`). На клиенте такие страницы должны исключаться из **useEffect** контейнера **Seo** – **fetchSeo** для них выполняется в эффектах на страницах после готовности данных (см. как это сделано для страницы Example). Иначе запрос SEO будет происходить раньше запроса данных страницы и в шаблоны не загрузятся данные.

## Mobile Friendly Practices

Поисковые движки при ранжировании учитывают насколько сайт mobile-friendly. В частности, основываясь на аудитах Lighthouse:

1. Должен быть указан тег **meta name=«viewport»**. Он прописан в разметке сервера `server/template.js`
2. Размер шрифтов не должен быть менее 12px для > 60% текста.
3. Интерактивные области должны быть не менее 48x48px и находиться друг от друга не менее, чем через 8px

📚 [Make your page mobile-friendly](https://web.dev/lighthouse-seo/#make-your-page-mobile-friendly)

## HTML Структура

Использование семантичной разметки предоставляет поисковым движкам дополнительную информацию, позволяющую выстроить взаимосвязь контента.
Всегда предпочитайте HTML5 теги нейтральным div, если они применимы. Это полезно не только для SEO, но и для A11Y.

Используйте заголовки H1-H6 для построения дерева заголовков. Не должно быть пропуска уровней – разметка первична, оформление можно задать через стили.

На странице обязательно должен быть тег H1. При этом он может быть **визуально** скрыт через `<VisuallyHidden />`, если заголовка нет на макете. Заголовок H1 на странице должен быть строго один.

♿️ Дерево заголовков также используют пользователи AT в качестве меню.

📚 [The Importance of Heading Levels for Assistive Technology](https://webdesign.tutsplus.com/articles/the-importance-of-heading-levels-for-assistive-technology--cms-31753)

Графический контент должен содержать атрибут alt. Здесь поисковые движки работают аналогично AT.

Все внешние ссылки должны идти с атрибутом rel="nofollow», запрещающим их индексацию.

💬 Компонент `Button` из GDS при передаче пропса `external` прописывает для ссылок **rel=«nofollow noopener» target=«\_blank»**

## HTTP Code

Фронтенд отвечает за возврат корректного кода ответа на серверном рендере.

Для этого в конфиге роутера доступно поле `status`. Указывайте его для кодов, отличных от 200. Например, для страницы 404 задаётся `status: 404`.

📚 [HTTP response status codes](https://developer.mozilla.org/en-US/docs/Web/HTTP/Status)

## Редиректы

Также фронтенд отвечает за проставление редиректов на уровне приложения.

Редиректы добавляются через компонент `Redirect`. Используется переопределённый компонент RWBP вместо стандартного компонента `react-router`, т.к. он дополнительно передаёт статус коды для серверного рендера. Статус по умолчанию равен 301, но его можно переопределить через проп `status`.
