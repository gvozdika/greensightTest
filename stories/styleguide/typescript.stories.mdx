import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / Typescript" />

# Typescript

RWBP и GDS написаны на Typescript. Typescript добавляет статическую типизацию и позволяет избавиться от целого пласта багов, связанных с типами. У нас он используется прежде всего, чтобы упростить долгосрочную поддержку проектов, и сделать работу разработчиков комфортнее за счёт автосаджестов IDE.

📚 Подробнее о бенефитах Typescript: [Why TypeScript is the best way to write Front-end in 2019](https://medium.com/@jtomaszewski/why-typescript-is-the-best-way-to-write-front-end-in-2019-feb855f9b164)

Наша задача при работе с Typescript: прописывать типы, делая tsc довольным, и по возможности не закидывая всё подряд в `any`. Чем больше вы напишете полноценных интерфейсов, тем лучше DX. Эдакий квест по угождению компилятора. Здесь важно соблюдать баланс между детальностью типов и эффективностью. Прежде чем прописывать сложные дженерики подумайте даст ли вам подобная типизация выигрыш в дальнейшем или этот код никто не использует. Прописывание типа `any` практически всегда может исправить ошибку – пусть эта мысль греет вам душу, однако проставление такого типа не многим лучше добавления нотации `eslint-ignore`, т.к. по сути вы просто отключаете типизацию для данного фрагмента кода.

Стоит отметить, что Typescript – умный парень и большинство типов определяет сам implicitly, в чём вы не раз ещё убедитесь. Но порой ему требуется наша поддержка. Данный гайд содержит набор практических советов, позволяющих упростить работу с Typescript.

## Validation

Все создаваемые файлы должны иметь формат `.ts` для обычного кода и `.tsx` для кода с jsx. Эти файлы должны быть написаны на валидном Typescript, соблюдение которого проверяется через сочетание компилятора tsc и ESLint. Первый производит статический анализ кода, второй по большей части указывает на ошибки в кодстайле.

Ручной запуск tsc:

```bash
yarn tsc
```

Ручной запуск ESLint:

```bash
yarn eslint
```

Обе проверки зашиты на precommit hook через `husky` + `lint-staged`, что не позволит вам закоммититься при наличии проблем. Кроме того production сборка использует tsc для сборки серверного кода, так что всегда проверяйте, что код собирается, и на сервере не будет сюрпризов.

Помимо ручного запуска tsc и ESLint поддерживаются всеми современными редакторами кода. Особенно хорош в этом плане VSCode, т.к. его как и Typescript делает Microsoft. Настройка среды под VSCode описана в гайде **Linting**. Убедитесь, что ваш редактор корректно настроен, чтобы получить максимум от статической типизации.

## Settings

Настройки Typescript прописаны в `tsconfig.json`. Этот конфиг не создаёт никаких файлов и нацелен на проверку кодбазы в strict моде. Дополнительно есть `tsconfig.server.json` – этот файл предназначен для генерации js файлов для серверного кода на production. Это необходимо, т.к. запуск ts-файлов в node-окружении напрямую невозможен без инструментов вроде `ts-node`, которые предназначены исключительно для разработки. Именно он используется у нас в dev-моде.

Обработкой ts-файлов клиентского кода занимается `babel-loader`, что стало возможным с пресетом `@babel/typescript`. Линтинг производит `ESLint` через `@typescript-eslint/parser` вкупе с `@typescript-eslint` плагином.

💬 Историческая справка: ещё не так давно для обработки ts приходилось подключать `ts-loader` или `awesome-typescript-loader`, а для линтинга подключать `TSLint`. Сейчас в первых уже нет необходимости, а второй в статусе deprecated.

💬 Опция `"esModuleInterop": true` убирает необходимость писать импорты в формате `import * as React`. В подавляющем числе случаев импорты пишутся как обычно. К исключениям можно отнести разве что webpack.

## Global declarations

На проекте всего два d.ts файла, и они лежат в корне src: `module.d.ts` и `global.d.ts`. Все остальные интерфейсы / типы пишутся напрямую в ts-файлах.

💬 d.ts – это формат деклараций Typescript, которые может генерировать tsc на основании ваших ts-файлов. Обычно нет необходимости писать их вручную.

`module.d.ts` содержит декларации модулей, т.е. типы приходящие из ESM импортов. Здесь указываются типы для файлов, обычно не являющихся модулями (вне окружения webpack), как файлы изображений, и для внешних зависимостей, имеющих проблемы с тайпингами. О зависимостях подробнее в разделе **Dependencies**.

`global.d.ts` содержит глобальные декларации. Эти объявления доступны на всём проекта без необходимости их импортировать. Таким же образом работают некоторые из зависимостей, как React – его типы доступны повсеместно. Обычно прописывать свои вам не нужно за исключением ряда хаков вроде расширения объекта window.

## Dependencies

Используемые на проекте внешние библиотеки могут быть написаны на TS или на JS. В первом случае библиотека поставляет собственные файлы деклараций d.ts (как GDS), и вы можете обмазываться автосаджестами вашего любимого редактора и не иметь проблем.

Во втором случае народные умельцы создают отдельные модули с типами @types – необходимо поставить актуальную вашей версии библиотеки версию типов, и всё опять же заработает. Все подобные модули типов хранятся в [DefinetelyTyped](https://github.com/DefinitelyTyped/DefinitelyTyped) и существуют для всех уважаемых библиотек.

Для автоматизации процесса подключения @types для JS-библиотек на проект установлен `typescript` плагин для yarn (см. `yarnrc.yml`). Он автоматически добавит пакет типов в devDependencies при добавлении зависимости через `yarn add`.

💬 Соглашение: все зависимости RWBP хранятся в `dependencies` и только @types лежат в `devDependencies`. Такой подход упрощает установку зависимостей на проекте, где в отличие от библиотеки разделение dependencies/devDependencies не играет роли. Также обратите внимание на то, что у @types нет знака `^` в номере зависимости, т.е. версия зашита, т.к. обновление типов чревато возникновением проблем из-за отсутствия синхронизации с модулем.

Порой возникают васянские библиотеки на JS, для которых типы в принципе не существуют. Это прежде всего должно быть поводом задуматься нужна ли вам такая библиотека. Если всё-таки да, то вы можете обойти ошибку tsc, добавив собственную декларацию в `module.d.ts`. В простейшем варианте:

```ts
declare module 'vasyan-library';
```

Такое определение просто уберёт ошибку о необъявленном модуле. Это не даст вам никаких полезных саджестов, но зачастую это лучший вариант, чтобы быть эффективными. Но при желании есть возможность расписать всю структуру экспортов:

```ts
declare module 'vasyan-library' {
    const value: string;
    export default value;
}
```

Самое сложное - при создании своих компонентов на базе компонентов внешней библиотеки предоставить своим пропсам библиотечные типы. Для этого приходится искать форматы типов в документации библиотеки или типах DefinetelyTyped, чтобы унаследовать свои пропсы от библиотечных и не вызвать конфликтов между типами. В случае крутых библиотек вроде `Formik` или `Downshift` с этим проблем нет, т.к. все нужные типы можно экспортировать, а их применение довольно прозрачно диктуется документацией. Но чем васяннее библиотека, тем больше возникает проблем. Именно несколько библиотек перекрыты декларациями из `module.d.ts`.

## TSDoc

[TSDoc](https://github.com/Microsoft/tsdoc) – это стандарт комментариев в Typescript документациях. Очень похож на JSDoc, ознакомиться с его возможностями можно на [TSDoc Playground](https://microsoft.github.io/tsdoc/). Внутри можно изменять начертания, отмечать переменные, давать ссылки и выводить сниппеты кода.

`TSDoc` повсеместно используется в GDS – именно поэтому вы видите развёрнутые описания при наведении на импортируемые оттуда модули. Например при определении объекта темы. На уровне проекта нет такой необходимости оставлять много комментариев. Достаточно описывать пропсы в интерфейсах – эти комментарии отобразятся при использовании пропсов компонента и будут вынесены в таблицу пропсов Storybook.

💬 Соглашение: все интерфейсы пишутся на английском языке. Соответствуем международному стандарту, чтобы не было проблем с участием в мультиязычных группах вроде ADEO.

## Components

Пропсы всех компонентов должны быть описаны внутри файла с кодом компонента через интерфейс, который должен быть доступен на экспорт. Формат интерфейсов описан в документации [Interfaces](https://www.typescriptlang.org/docs/handbook/interfaces.html). Пример интерфейса:

```ts
export interface ExampleProps {
    /** Your name */
    name?: string;
    /** Button click handler */
    onClick?: React.MouseEventHandler<HTMLButtonElement>;
}
```

💬 Соглашение: интерфейсы пропсов называть по имени компонента с суффиксом "Props". Для прочих интерфейсов и типов достаточно имени с заглавной буквы. I-нотация запрещена на уровне линтера.

React компоненты – это обычные функции, так что присвоение интерфейса выглядит очень просто:

```tsx
const Example = ({ name, onClick }: ExampleProps) => {
    return <Button onClick={onClick}>Hello, {name}</Button>;
};
```

💬 Вы можете встретить объявление компонентов через `React.FC<ExampleProps>` – он чуть более нагружен, и практически не даёт никаких бенефитов, так что приведённый выше метод предпочтительнее.

Если компонент проектируется в Storybook, то этот интерфейс становится доступен для вывода в виде таблицы пропсов, благодаря `react-docgen-typescript-loader`:

```md
<Props of={Example} />
```

### Extending HTML Elements

Если ваш компонент принимает пропсы некоторого стандартного компонента, к примеру рендерится через тег `<button />` и пробрасывает в него пропсы через `{...props}`, то ваш интерфейс должен наследовать пропсы этого элемента.

Пропсы стандартных элементов определяются через дженерик `React.HTMLProps` с именем элемента в качестве параметра. Имена обычно начинаются с "HTML" – здесь автосаджесты помогут. Например для кнопки:

```ts
export interface ExampleProps extends React.HTMLProps<HTMLButtonElement> {
    /** Your name */
    name?: string;
}
```

💬 Обратите внимание, что при наследовании вам уже не нужно определять самим встроенные пропсы вроде `onClick`.

💬 В Typescript поддерживается множественный extends через запятую.

В Storybook в настройках `react-docgen-typescript-loader` задан фильтр пропсов таким образом, что встроенные / внешние типы в таблицы не выводятся, т.к. иначе их было бы слишком много. Просто указывайте факт расширения стандартного компонента словами, а в случае расширения библиотеки, отсылайте в её документацию.

### Compound Components

Если вы используете паттерн Compound Components, то возникает проблема с типами при добавлении сабкомпонентов через точку – у компонента появляются недокументированные параметры, которые при этом нет возможности указать в интерфейсе пропсов, т.к. вообще говоря это не пропсы, а мы просто используем функцию в качестве объекта.

Для решения этой проблемы такие компоненты описываются несколько иначе. На примере `Accordion`:

```ts
interface AccordionCompositionProps {
    Item: React.FC<AccordionItemProps>;
    Heading: React.FC<AccordionHeadingProps>;
    Panel: React.FC<AccordionPanelProps>;
    Button: React.FC<AccordionButtonProps>;
}

interface AccordionProps extends AccordionContext {
    /** List of Accordion.Item components */
    children: React.ReactNode;
}

const Accordion: React.FC<AccordionProps> & AccordionCompositionProps = props => {};
```

💬 Для композиционных пропсов допускается опускание TSDoc.

Здесь мы описываем композицию отдельным интерфейсом, представляя каждый сабкомпонент через `React.FC`, описываем пропсы самого компонента, как обычно, и используем интерсекцию на уровне компонентов.

Внимательный читатель нашего гайда мог заметить `AccordionContext` – это второй концепт, достойный упоминания. Compound component зачастую определяет контекст и передаёт часть своих пропсов (или вычисляемых из них параметров) дочерним элементам через `Provider`. Именно поэтому пропсы в этом примере наследуются от интерфейса с пропсами контекста.

Рекомендуемым подходом является определение кастомного хука для использования контекста. Его содержимое может выглядить следующим образом:

```ts
export interface AccordionContextProps {
    /** Some example value */
    exampleValue: string;
}

export const AccordionContext = createContext<AccordionContextProps | undefined>(undefined);

const useAccordion = (): AccordionContextProps => {
    const context = useContext(AccordionContext);

    if (!context) {
        throw new Error('This component must be used within a <Accordion> component');
    }

    return context;
};

export default useAccordion;
```

Хук определяется в отдельном файле во избежание circular dependencies. Модуль предоставляет интерфейс и контекст для использования в родительском компоненте и хук для использования в дочерних компонентах без использования импортировать контекст. Кроме того хук добавляет проверку на использование дочерних компонентов вне родителя.

Чтобы документировать интерфейс составного компонента в Storybook используйте запись с множественным выводом компонентов – она представит пропсы в виде табов:

```md
<Props components={{ Example, 'Example.Item': Example.Item }} />
```

💬 Чтобы пропсы для compound components распознавались `docgen` нужно помимо дефолтного экспорта экспортировать компоненты обычным экспортом. Общая рекомендация: если компонент составной, то писать `export const` и дефолтный, а если одиночный, то только дефолтный.

📚 Подробнее про паттерн в контексте Typescript: [Compound React Components with Hooks + TypeScript](https://blog.martindidiego.com/compound-components-typescript/)

## Common Types

Все базовые типы описаны в документации [Basic Types](https://www.typescriptlang.org/docs/handbook/basic-types.html). Типы примитивов пишутся со строчной буквы. Для объектов писать `Object` или `{}` в интерфейсах бессмысленно – вместо этого описывайте интерфейс объектным литералом или другим интерфейсом. Писать Function также бессмысленно - необходимо предоставить типы аргументов и возвращаемых значений: `(id: number) => void`. Типизированные массивы любой длины пишутся как `string[]` или `Array<string>`, фиксированной `[string, string]`.

[Intersection type](https://www.typescriptlang.org/docs/handbook/advanced-types.html#intersection-types) – это `type T = K & I`, объединение двух типов в один. [Union type](https://www.typescriptlang.org/docs/handbook/advanced-types.html#union-types) – это `type T = K | I`, выбор из двух типов. Через union зачастую делают перечисления констант, например `type: 'grid' | 'flex'` вместо более широкого `type: string`.

Работа с функциями описана в документации [Functions](https://www.typescriptlang.org/docs/handbook/functions.html). Самое главное, что следует сделать при разработке функции – это описать типы её аргументов, иначе они будут равны any, что strict mode запрещает (опция noImplicitAny). Второе – это убедиться, что Typescript правильно рассчитал тип возвращаемого значения, и помочь ему, если нет. Обычно это достигается через доработку тела функции, и явно указывать тип возвращаемого значения не требуется.

В большинстве случаев нет особого смысла использовать rest для аргументов функции, не считая отдельных кейсов вроде получения максимума из энного количество элементов одного типа. В любом случае придётся уточнять их типы в виде массива, так что лучше задать их явно через переменные. Например, вы расширяете хэндлер некоторого плагина, и в конце вам нужно вызвать оригинальный метод – вам в любом случае придётся определить типы всех аргументов, взяв их из документации плагина.

Если у вас есть объект `example` и вы хотите получить его тип, то используйте `type Example = typeof example`. Если у вас есть интерфейс `Example`, а вы хотите получить тип с перечислением его пропсов, то используйте `type ExampleProps = keyof Example`. Как вы можете догадаться, можно получить пропсы и прямо из объекта: `type ExampleProps = keyof typeof example`.

Если вы определили интерфейс, а вам нужен тип отдельного поля из него, то его можно получить через квадратные скобки: `Example[name]`.

Для более сложных типов используются дженерики: [Generics](https://www.typescriptlang.org/docs/handbook/generics.html). По сути это параметризированные типы. Прописывать собственные на уровне приложения приходится довольно редко, но вот в библиотеках они постоянно используются, так что нужно понимать как они работают.

### React Specific Types

Для `children` используйте `React.ReactNode`. Для `style` используйте `React.CSSProperties`, для `css` импортируйте `CSSObject` из `@emotion/core`. Хотя если только вы не работаете над библиотекой, типы для `css` и так доступны.

Хэндлеры - это дженерики с именем, содержащим "Handler" в названии, с параметром, представляющем тип `event.target`. Например для обработчика клика по кнопке: `React.MouseEventHandler<HTMLButtonElement>`. Если проп хэндлера инлайнится, то это всё что нужно. Если идёт из переменной, то нужно уточнить в нём формат эвента, скопировав из интерфейса забиваемого хэндлера: `(e: React.MouseEvent<HTMLButtonElement>)`.

`useState` берёт тип по дефолтному значению. Если его нет, то необходимо предоставить через дженерик и явно указать неизвестный тип: `React.useState<IUser | null>(null)`.

`useRef` без initial value также оформляют через дженерик: `React.useRef<HTMLElement>(null)`. Главное делать проверки на существование `current`. Если `current` мутабельный (назначение в колбеке), то меняете параметр дженерика на `<HTMLElement|null>`.

`useEffect` должен возвращать либо функцию (очистка), либо undefined (ранний выход). Здесь Typescript типы проставит сам.

`Context` задаётся через дженерик `createContext<IAccordionContext | undefined>(undefined)`.

`forwardRef` определяется через дженерик:

```ts
const Button = forwardRef<HTMLButtonElement, ButtonProps>((props, ref) => {});
export default Button;
```

Если нужно разделить определение компонента от экспорта обёртки в `forwardRef` (например, `docgen` не может обработать пропсы для вывода в Storybook), то таким образом:

```ts
const Button = (props: ButtonProps, ref: React.Ref<HTMLButtonElement>) => {};
export default forwardRef(Button);
```

### Utility Types

Typescript определяет ряд [Utility Types](https://www.typescriptlang.org/docs/handbook/utility-types.html) – дженерики, служащие для получения из ваших типов новых типов, например при помощи опускания ряда параметров или изменяя их обязательность. На личном опыте самые часто используемые из них: `Omit`, `Record` и `Partial`.

#### Omit

Используется для исключения отдельных параметров из переданного типа. Можно исключать несколько параметров через union. Частый пример использования: наследуемся от стандартного элемента, переопределяем стандартный параметр и имеем конфликт в типах. В таком случае разумно удалить этот параметр из наследуемого типа. Например, для табов:

```ts
export interface TabsProps extends Omit<React.HTMLProps<HTMLDivElement>, 'onSelect'> {
    onSelect?: (index: number, last: number, event: Event) => boolean | void;
}
```

#### Record

Это удобная запись для объекта с набором полей определённого типа. Например, в GDS мы имеем набор доступных имён брейкпоинтов и позволяем задавать пропсы в виде объектов с различными значениями на разных брейкпоинтах. Мы бы могли описать такой проп в следующем формате:

```ts
interface Example {
    /** Your name */
    name?: {
        xxxl: string;
        xxl: string;
        xl: string;
        lg: string;
        md: string;
        sm: string;
        xs: string;
        xxs: string;
        xxxs: string;
    };
}
```

Но вместо этого определим тип брейкпоинта и используем `Record`:

```ts
type Breakpoint = 'xxxl' | 'xxl' | 'xl' | 'lg' | 'md' | 'sm' | 'xs' | 'xxs' | 'xxxs';
interface Example {
    /** Your name */
    name?: Record<Breakpoint, string>;
}
```

#### Partial

Делает все поля необязательными. Например, использование `Record` в предыдущем примере сделало указание каждого брейкпоинта обязательным, мы же хотим дать возможность задать любое сочетание брейкпоинтов. Правится очень просто – оборачиваем `Record` в `Partial`: `Partial<Record<Breakpoint, string>>`.

Можно пойти дальше и создать тип, позволяющий в качестве пропса передать значение напрямую или в формате объекта с брейкпоинтами:

```ts
type Breakpoint = 'xxxl' | 'xxl' | 'xl' | 'lg' | 'md' | 'sm' | 'xs' | 'xxs' | 'xxxs';
type AllowMedia<T> = T | Partial<Record<Breakpoint, T>>;
interface Example {
    /** Your name */
    name?: AllowMedia<string>;
}
```

Именно так работает компонент `Layout` в GDS.

`Partial` не позволяет сделать необязательными только часть параметров, но для этого несложно написать собственные типы:

```ts
type PartialBy<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
type RequiredBy<T, K extends keyof T> = Omit<T, K> & Required<Pick<T, K>>;
```

Остальные утилиты для жонглирования типами смотрите в документации.

### Type Guards

Для проверки на существование поля не хватит обычных `if` или `optional chaining`. Используйте `in` для проверки на наличие поля в объекте, `typeof` на соответствие примитивам, `instanceof` для соответствия конструкторам. Например:

```ts
type IImage = string | IResponsiveImage | ILazyImage;
if (typeof image !== 'string' && 'dimensions' in image) console.log(image.dimensions);
```

Можно пойти дальше и написать собственный [Type Guard](https://www.typescriptlang.org/docs/handbook/advanced-types.html#type-guards-and-differentiating-types) – это функция, возвращающая `type predicate`. На примере тех же изображений:

```ts
const isResponsive = (image: IImage): image is IResponsiveImage => typeof image !== 'string' && 'srcset' in image;
```

## Redux

Работа с Redux, как и всё с ним связанное, можно описать чёткой последовательностью действий:

1. Определить функции маппинга в контейнере, стейт отметить как `RootState`
1. Создать коннектер и получить Redux props через `ConnectedProps`
1. Создать type, объединяющий Redux props с обычными, и присвоить этот тип контейнеру
1. В редусере всем селекторам присвоить `RootState`
1. Объявить `initialState` через интерфейс
1. Прописать типы экшенов через `PayloadAction`
1. Прописать `AppThunk`, если используется `redux-thunk`

## Life Hacks

Несколько непопавших под категории лайфхаков для выхода из "безвыходных" положений:

1. В моменты отчаяния помогает [Type assertion](https://www.typescriptlang.org/docs/handbook/basic-types.html#type-assertions) – он напрямую говорит компилятору воспринимать переменную в качестве указанного типа. Используйте её в качестве крайней меры, т.к. обычно это скорее является дурным тоном, но порой без неё не обойтись. Например в контексте GDS, можно передать baseTheme в createTheme через `baseTheme as any`, т.к. вообще говоря базовая тема задаётся в качестве константы и не приводится к требуемому типу `Theme`. Через `as` можно даже присваивать в объекты недокументированные поля: `(TabsList as any).tabsRole = 'TabList'`
1. Если tsc выводит сотню ошибок `@types/react`, то вероятнее всего они появились из-за различий в версиях. Введите `yarn why @types/react` – если там показаны разные версии, то проблема именно в этом. Помогает переустановка с удаление `yarn.lock`
1. Если саджест определяет импорты, и интерфейс корректно прописывается, но линтер говорит, что interface not found, то может помочь: `import * as Example`

## Useful Resources

-   📚 [Typescript Docs](https://www.typescriptlang.org/docs/home.html) – официальная документация. Прежде всего раздел **Handbook**.
-   📚 [React+TypeScript Cheatsheets](https://github.com/typescript-cheatsheets/react-typescript-cheatsheet) – рекомендации по использованию Typescript в контексте React.
