import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Styleguide / Webpack" />

# Webpack

Проект построен на сборщике `Webpack`. Точкой входа для него служит файл `webpack.config.js` в корне проекта. Внутри него прописаны общие настройки, которые распространяются на dev/prod сборку клиентских и серверных конфигов. В этот конфиг при запуске сборки передаётся переменная `isClient` и читается окружение из `NODE_ENV` - в зависимости от этих значений объект с общими настройками расширяется дополнительными правилами, мержась с соответствующими конфигурациями из `config`.

В `config` прописываются все дополнительные конфиги и директория `helpers` со вспомогательными функциями, задействованными в конфигах.

☝🏿 webpack не запускается напрямую через скрипты. Вместо этого сборка происходит программно из серверного кода (см. `devServer.js` и `prodServer.js`).

## Client / Server

В проекте идёт разделение на **клиентскую** и **серверную** сборку.

**Клиентский** конфиг получается объединением корневого конфига с базовым клиентским и клиентским конфигом среды. Точкой входа для него служит файл `src/client/index.js`, на выходе получается наполнение директории `build` скриптами, стилями и ассетами. Т.е. это непосредственно клиентское приложение.

**Серверный** конфиг получается объединением корневого конфига с серверным конфигом (он для обеих сред единый). Точкой входа для него служит файл `src/server/render.js`, на выходе получается один единственный файл в директории `build` - `server.js`. Этот файл исполняется при запросе к сайту в браузере, выполняя первичный серверный рендер и возвращая шаблон.

Сборка клиентской и серверной части происходит параллельно, ввиду их независимости.

☝🏿 В серверном коде происходит импорт клиентского приложения, его рендер (суть SSR). Поэтому **ВСЕ** импорты клиентского приложения должны быть обработаны, принимая во внимание серверное окружение, в котором многие возможности недоступны. Поэтому серверная сборка помимо прогонки через `babel` исключает обработку `node_modules` через `node-externals`, скипает обработку css через `null-loader`, по возможности не пишет ассеты на диск и так далее.

## Dev / Prod

В проекте идёт разделение на **development** и **production** сборку.

**Development-сборка** нацелена на скорость и удобство разработки, активируя максимум девелоперских инструментов и отключая при этом всю оптимизацию. На сервере используется `webpack-dev-middleware`, позволяющий выполнять автоматическую пересборку изменённого кода на сейв. Более того работает `Hot Module Replacement` (HMR), позволяющий обновлять css и js без перезагрузки страницы. Также в dev-моде доступны Redux Devtools и sourcemaps.

**Production-сборка** - полная противоположность. Она генерирует итоговый бандл, включающий все оптимизации и отключающий функции live reload и прочие удобства разработчика. Эта сборка может занимать приличное время, т.к. фокус здесь уже идёт на качество финального бандла, а не на скорость сборки. Для запуска приложения на сервере используется `Express static`.

☝🏿 Важно, что development сборка функционально не отличается от production, т.е. на ней тоже работает SSR и Code Splitting, что значительно понижает шансы того, что рабочий на development код не запустится на production.

## Работа сборки

В качестве конечной директории вне зависимости от окружения используется директория `build`.

Директория `build` находится в .gitignore, чтобы не засорять репозиторий файлами, которые можно сбилдить. Сервер выполняет пересборку сам после пуша в репозиторий.

Для оптимизации времени сборки не выполняется полного удаления `build` на старте и происходит кэширование результатов выполнения ряда тяжеловесных лоадеров/плагинов. Данные кэша хранятся в папке `node_modules/.cache`.
